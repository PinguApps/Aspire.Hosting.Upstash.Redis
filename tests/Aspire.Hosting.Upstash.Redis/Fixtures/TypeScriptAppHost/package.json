{
  "name": "pinguapps-upstash-redis-typescript-apphost-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "restore": "aspire restore --non-interactive",
    "start": "aspire start --non-interactive --isolated",
    "wait:cache": "aspire wait cache --status healthy --timeout 120 --non-interactive",
    "stop": "aspire stop --non-interactive",
    "publish": "aspire publish --non-interactive --list-steps",
    "typecheck": "tsc -p tsconfig.apphost.json --noEmit"
  },
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >=24"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "tsx": "4.23.1",
    "vscode-jsonrpc": "8.2.1",
    "typescript": "7.0.2"
  }
}
