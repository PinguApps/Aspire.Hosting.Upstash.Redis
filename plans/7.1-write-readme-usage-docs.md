# Write README Usage Docs

Step: `7.1`  
Stage: `7`  
Depends on: `1.1`, `2.3`, `5.1`, `5.2`, `5.3`  
Can run in parallel with: `7.2`  
Blocks: `7.3`

## Purpose
Turn the package from "implemented" into "usable by strangers." This task exists to write the release-quality README that explains exactly what the package does, how to use it, and what guarantees and limitations it has.

## Full Description
Replace the placeholder README with real documentation for the package. The README must be written for an OSS consumer who understands Aspire but knows nothing about this package yet.

It must cover:

- what the package is for
- supported versions
- the fact that local development still uses standard Aspire Redis behavior
- how Upstash deployment is opted into
- the three ownership modes
- the recommended parameter pattern for Upstash email, API key, and explicit remote database name
- the `UpstashRedisValue` literal-or-parameter source model for optional string settings
- the supported optional settings
- the app-facing outputs
  - include the task `5.2` supplementary output accessor (`GetUpstashRedisOutputs()`), stable names, and password-only secret classification
  - distinguish those supplementary outputs from the standard Redis connection-string output owned by `5.1`
- the "never auto-delete" behavior
- how repeated deploys behave
- important limitations and safety notes
- the native Upstash account requirement for the management API

The README should also include at least one canonical, polished sample for the expected happy path and concise sections for the less common modes. Keep it precise and practical. Do not over-document hypothetical future features.

All code snippets must be accurate against the real API. If a sample project or snippet source exists from `7.2`, align the README with it exactly.

Repository state rule: before the PR for this task is created, update any affected files in `plans/` including this file and any other plan files so they reflect the new repo state accurately. Keep `AGENTS.md` and `README.md` up to date in the same PR whenever the task changes behavior, workflow, structure, constraints, usage, or any other documented repo state. No PR from this task is complete until the implementation, tests, `README.md`, `AGENTS.md`, and all relevant plan files describe the same current reality.

## Expected Deliverables
- A complete README replacing the placeholder file.
- Accurate usage examples for common and advanced cases.
- Clear explanation of security boundaries and deployment behavior.

## Testing Requirements
- Validate every code snippet against the implemented API, preferably by aligning it to a real compiled sample or a test-backed snippet source.
- Run the full test suite and `dotnet build ./Aspire.Hosting.Upstash.Redis.slnx`.
- If any snippet cannot be compile-validated directly, explain how it was verified in the PR.

## PR Requirements
The PR must:

- Summarize the README structure and the main user journeys it covers.
- State how snippet accuracy was verified.

## Plan Maintenance Requirements
If writing docs reveals missing sample or API clarification work:

- Update `7.2` or earlier plan files in the same PR.

## Done When
- A new consumer can install the package, understand the deployment contract, and use it correctly from the README alone.

## Current Repo Note
- Task `7.2` has added compile-validated AppHost snippets in `samples/AppHostSnippets/UpstashRedisAppHostSnippets.cs`; use that file as the source of truth for README usage examples where practical.
