# Add Sample AppHost Snippets

Step: `7.2`  
Stage: `7`  
Depends on: `1.1`, `5.1`, `5.2`  
Can run in parallel with: `7.1`  
Blocks: `7.3`

## Purpose
Provide concrete, validated examples that make the package easier to trust, document, and maintain.

## Full Description
Add the sample usage material needed to demonstrate the final package behavior clearly. This task may take the form of:

- a small sample AppHost project under a `samples/` folder
- a validated snippet source used by docs and tests
- another lightweight approach that keeps examples compile-accurate

Choose the smallest correct artifact that gives the repo trustworthy examples. The examples must cover:

- the canonical create-or-adopt flow
- create-only
- existing-only
- parameterized management credentials
- parameterized explicit remote database name
- consuming project reference via `WithReference(cache)`

The sample material should be useful for both humans and maintainers. Ideally it should also reduce the chance of docs drift by giving `7.1` a concrete source of truth.

Repository state rule: before the PR for this task is created, update any affected files in `plans/` including this file and any other plan files so they reflect the new repo state accurately. Keep `AGENTS.md` and `README.md` up to date in the same PR whenever the task changes behavior, workflow, structure, constraints, usage, or any other documented repo state. No PR from this task is complete until the implementation, tests, `README.md`, `AGENTS.md`, and all relevant plan files describe the same current reality.

## Expected Deliverables
- A validated example source for the package's intended AppHost usage.
- Coverage of all three ownership modes and parameterized credentials.
- Alignment with README snippets.

## Testing Requirements
- Ensure the sample or snippet source is validated through build or test coverage.
- Run the full test suite and `dotnet build ./Aspire.Hosting.Upstash.Redis.slnx`.
- If a full sample project is added, ensure it builds cleanly without introducing unrelated maintenance burden.

## PR Requirements
The PR must:

- Explain the chosen sample strategy and why it is the smallest correct one.
- Show how it aligns with the README and package API.

## Plan Maintenance Requirements
If maintaining sample accuracy requires future explicit work:

- Update the corresponding plan files in `plans/` in the same PR.

## Done When
- The repo contains reliable, validated example usage that future docs and contributors can follow.
