# Add Tests for Local No-Op and API Shape

Step: `6.1`  
Stage: `6`  
Depends on: `1.1`, `5.1`, `5.3`  
Can run in parallel with: `6.2`, `6.3`  
Blocks: `7.3`

## Purpose
Harden the highest-risk user-facing contract that should never regress: adding this package must not ruin normal local Redis usage, and the public API should behave exactly as designed.

## Full Description
Expand the Reqnroll suite to focus specifically on the local-development contract and the final public API shape. Earlier tasks should already have added targeted tests as they introduced code; this task is the dedicated hardening pass that fills the remaining gaps and makes those core guarantees exhaustive.

The scenarios added here should cover:

- no Upstash activity when the resource is not annotated for Upstash publish behavior
- no Upstash activity during normal local execution semantics
- standard Redis references still work
- the final fluent API is discoverable and behaves consistently across overloads and modes
- management credentials remain separate from app-facing outputs

This task should be test-first where meaningful. If it finds product bugs while writing coverage, it should fix them in the same PR, but only insofar as needed to satisfy the contract under test.

Repository state rule: before the PR for this task is created, update any affected files in `plans/` including this file and any other plan files so they reflect the new repo state accurately. Keep `AGENTS.md` and `README.md` up to date in the same PR whenever the task changes behavior, workflow, structure, constraints, usage, or any other documented repo state. No PR from this task is complete until the implementation, tests, `README.md`, `AGENTS.md`, and all relevant plan files describe the same current reality.

## Expected Deliverables
- Comprehensive Reqnroll coverage for local no-op semantics and final API shape.
- Any focused bug fixes required to make those tests pass.

## Testing Requirements
- Add or expand scenarios for:
  - plain `AddRedis` remains unaffected
  - `AddRedis(...).PublishToUpstash(...)` still preserves local reference behavior
  - public API overloads and ownership modes are represented correctly
  - no management secret leaks into consuming-app paths

Task `1.1` already added baseline Reqnroll coverage for the locked API shape: all three ownership modes, required-value validation, unsupported ownership validation, disabled TLS validation, parameter-based required inputs, and parameter-based optional string settings. This task should expand from that baseline rather than duplicating the same narrow checks.

- Run the full test suite and `dotnet build ./Aspire.Hosting.Upstash.Redis.slnx`.

## PR Requirements
The PR must:

- State which local-behavior and API-shape risks were covered.
- Identify any bugs found while closing those coverage gaps.

## Plan Maintenance Requirements
If these tests expose missing implementation work:

- Update the corresponding task files in `plans/` in the same PR.

## Done When
- The package's local no-op and API-shape guarantees are heavily covered and stable.

## Completion Notes
- Added dedicated Reqnroll coverage for plain `AddRedis` local behavior with no Upstash metadata, pipeline step, supplementary outputs, or fake-provider activity.
- Added local no-op coverage for `.PublishToUpstash(...)` before deploy, including normal `WithReference(cache)` behavior, no Upstash connection-output redirect, standard Redis connection properties, and management-secret exclusion from app-facing Redis output/reference paths.
- Added API-shape coverage across the literal-database/parameter-credential overload, parameter-database/parameter-credential overload, and literal `UpstashRedisValue` overload, verifying consistent ownership capture and same-builder fluent chaining.
- No product bugs were found while closing these coverage gaps.
- Validation run:
  - `dotnet test Aspire.Hosting.Upstash.Redis.slnx`
  - `dotnet build ./Aspire.Hosting.Upstash.Redis.slnx`

State: Complete
