## Rolling state
- Goal: Address PR feedback asking analyzer project settings to follow the root `Directory.*` marker-property pattern.
- Current plan: Complete; analyzer-only settings now live in `Directory.Build.targets` under `IsAnalyzer`.
- Open questions/risks: `TargetFramework` remains in the analyzer csproj because project-set `IsAnalyzer` is not available when `Directory.Build.props` is imported.
- Next actions: None for this feedback.
- Key paths: `Directory.Build.targets`, `build/PinguApps.CodeAnalysis/PinguApps.CodeAnalysis.csproj`

## Session log
### 2026-06-06 02:33 Z (pr-4-feedback-3366377329-37bf9dd03e7e4c7e83e2aa830d1c8d59)
- Move analyzer build settings [build] (impact: low)
  - Why: Reviewer asked to centralize analyzer configuration under an `IsAnalyzer` condition, matching the test-project pattern.
  - Change: Added `IsAnalyzer`-conditioned analyzer properties/package reference to root targets; reduced analyzer csproj to `TargetFramework` plus `IsAnalyzer` marker. (files: `Directory.Build.targets`, `build/PinguApps.CodeAnalysis/PinguApps.CodeAnalysis.csproj`)
  - Notes: `dotnet build` and `dotnet test --no-build` passed; `TargetFramework` stayed local due to MSBuild import order.
