## Rolling state
- Goal: Complete PIN-155 / task 1.1 by locking the public Upstash Redis API shape.
- Current plan: Done; later tasks should build behind `.PublishToUpstash(...)`, `UpstashRedisOwnershipMode`, `UpstashRedisDeploymentOptions`, and `UpstashRedisValue`.
- Open questions/risks: Rich provider-domain validation remains intentionally deferred to task `2.3`.
- Next actions: Implement `2.1` internal state hardening, `2.3` domain validation/mapping, and `3.1` deploy-time value resolution behind the locked API.
- Key paths: src/Aspire.Hosting.Upstash.Redis/; tests/Aspire.Hosting.Upstash.Redis/Features/PublishToUpstash.feature; plans/1.1-lock-public-api-shape.md; README.md

## Session log
### 2026-06-06 14:38 Z (agent/pin-155-1.1---lock-public-api-shape)
- Lock public API shape [api/tests/docs] (impact: high)
  - Why: Task `1.1` needed a stable consumer surface before deploy logic tasks build behind it.
  - Change: Added `UpstashRedisValue`, parameter/literal `PublishToUpstash` overloads, explicit ownership validation, TLS-false validation, internal explicit-setting tracking, and Reqnroll coverage for ownership modes, parameter inputs, and invalid configuration. (files: src/Aspire.Hosting.Upstash.Redis/*, tests/Aspire.Hosting.Upstash.Redis/Features/*)
  - Notes: Synced README, AGENTS, decision notes, and downstream plans; validated with `dotnet build ./Aspire.Hosting.Upstash.Redis.slnx`, `dotnet test ./Aspire.Hosting.Upstash.Redis.slnx --no-build`, and `git diff --check`.
