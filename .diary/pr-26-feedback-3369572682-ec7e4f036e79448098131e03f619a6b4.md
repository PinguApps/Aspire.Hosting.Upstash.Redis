## Rolling state
- Goal: Address PR #26 feedback that supplementary `Password` output must expose secret metadata.
- Current plan: Rebase conflict resolved; keep rebase paused for user to continue.
- Open questions/risks: `UpstashRedisOutputReference` exposes package-local secret metadata; Aspire 13.4.2 has no generic secret flag on `ReferenceExpression`.
- Next actions: User can inspect staged conflict resolution and run `git rebase --continue`.
- Key paths: `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputReference.cs`, `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputs.cs`, `tests/Aspire.Hosting.Upstash.Redis/Steps/SupplementaryOutputsStepDefinitions.cs`

## Session log
### 2026-06-07 15:20 Z (pr-26-feedback-3369572682-ec7e4f036e79448098131e03f619a6b4)
- Fix password output secret metadata [api/tests/docs] (impact: med)
  - Why: PR feedback was valid; `ReferenceExpression` hid the private provider's secret flag from downstream consumers.
  - Change: Added public `UpstashRedisOutputReference` with `Name`, `ValueExpression`, and `Secret`; `UpstashRedisOutputs` now exposes those references and tests assert the password provider remains secret when wrapped in a `ReferenceExpression` (files: `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputReference.cs`, `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputs.cs`, `tests/Aspire.Hosting.Upstash.Redis/Steps/SupplementaryOutputsStepDefinitions.cs`, `README.md`, `plans/5.2-implement-supplementary-outputs.md`, `AGENTS.md`)
  - Notes: Verified with `dotnet test --no-restore`; Aspire 13.4.2 secret output support for Azure secrets uses Key Vault references, which is outside this Upstash v1 scope.
### 2026-06-07 15:24 Z (pr-26-feedback-3369572682-ec7e4f036e79448098131e03f619a6b4)
- Resolve rebase conflict [api/tests] (impact: low)
  - Why: Preserve both main's Redis resource reference tracking and PR feedback's public secret metadata wrapper.
  - Change: `UpstashRedisOutputReference` now owns the resource reference; `UpstashRedisOutputs` constructs public references from `RedisResource`; supplementary output tests assert references through `AsReferenceExpression()` (files: `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputReference.cs`, `src/Aspire.Hosting.Upstash.Redis/UpstashRedisOutputs.cs`, `tests/Aspire.Hosting.Upstash.Redis/Steps/SupplementaryOutputsStepDefinitions.cs`)
  - Notes: Verified with `dotnet restore` then `dotnet test --no-restore`.
