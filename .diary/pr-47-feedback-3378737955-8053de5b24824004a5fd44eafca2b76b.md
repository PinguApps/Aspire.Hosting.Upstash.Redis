## Rolling state
- Goal: Address PR #47 feedback about direct manual test workflow runs executing live Upstash scenarios.
- Current plan: Feedback validated; direct `_run-tests.yml` dispatch now defaults and falls back to non-live Reqnroll filter.
- Open questions/risks: `actionlint` is not installed locally; workflow syntax was inspected manually.
- Next actions: None.
- Key paths: `.github/workflows/_run-tests.yml`

## Session log
### 2026-06-09 07:41 Z (pr-47-feedback-3378737955-8053de5b24824004a5fd44eafca2b76b)
- Fix manual test workflow live filter [ci] (impact: low)
  - Why: Direct `workflow_dispatch` did not receive the `workflow_call` `test_filter` default.
  - Change: Added direct-dispatch `test_filter` default and PowerShell fallback to `Category!=live-upstash`. (files: `.github/workflows/_run-tests.yml`)
  - Notes: Verified fallback logic with `pwsh`; `git diff --check` passed.
