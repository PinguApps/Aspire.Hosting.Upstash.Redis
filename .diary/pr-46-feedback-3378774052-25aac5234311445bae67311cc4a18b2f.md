## Rolling state
- Goal: Address PR #46 feedback that TypeScript docs and samples used the legacy generated module path.
- Current plan: Done; docs, sample, fixture, config, and assertions now use Aspire 13.4 `apphost.mts` with `.aspire/modules/aspire.mjs`.
- Open questions/risks: Runtime `aspire restore` scenarios remain prerequisite-gated because the Aspire CLI is not installed in this environment.
- Next actions: None for this feedback unless reviewers want legacy-layout wording instead of current-layout samples.
- Key paths: `README.md`; `docs/getting-started-typescript.md`; `samples/TypeScriptAppHost/apphost.mts`; `tests/Aspire.Hosting.Upstash.Redis/Fixtures/TypeScriptAppHost/apphost.mts`

## Session log
### 2026-06-09 07:54 Z (pr-46-feedback-3378774052-25aac5234311445bae67311cc4a18b2f)
- Align TypeScript module path [docs/tests] (impact: med)
  - Why: Aspire 13.4 docs use `apphost.mts` and generated `.aspire/modules/aspire.mjs`; `.modules/aspire.js` is legacy for pre-13.4 AppHosts.
  - Change: Renamed maintained TypeScript sample and fixture to `apphost.mts`, updated imports/config/tsconfig/docs/tests to `.aspire/modules/aspire.mjs`, and removed stale `.modules` guidance. (files: `README.md`, `docs/*`, `samples/TypeScriptAppHost/*`, `tests/Aspire.Hosting.Upstash.Redis/*` | cmds: `dotnet test tests/Aspire.Hosting.Upstash.Redis/Aspire.Hosting.Upstash.Redis.Tests.csproj --filter "FullyQualifiedName~AppHostSnippets|FullyQualifiedName~TypeScriptAppHostFixture"`, `git diff --check`)
  - Notes: Focused test slice passed 4/7 with 3 Aspire CLI prerequisite-gated skips; `aspire` is not installed locally.
